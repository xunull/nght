nght is running!

Try:
  kubectl port-forward svc/{{ include "nght.fullname" . }} 8080:{{ .Values.service.port }}
  curl http://localhost:8080/echo/hello

Liveness probe (k8s):
  livenessProbe:
    httpGet: { path: /livez, port: http }

Readiness probe (k8s):
  readinessProbe:
    httpGet: { path: /health, port: http }
